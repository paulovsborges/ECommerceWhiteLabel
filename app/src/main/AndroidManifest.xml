<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.pvsb.ecommercewhitelabel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECommerceWhiteLabel"
        tools:targetApi="31">
        <activity
            android:name=".presentation.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.activity.ActivityProductDetails" />
        <activity android:name=".presentation.activity.ActivityProductFilters" />
        <activity android:name=".presentation.activity.ActivityPayment" />
        <activity android:name=".presentation.activity.ActivityCreateAccount" />
        <activity android:name=".presentation.activity.ActivityUserRegistration" />
        <activity android:name=".presentation.activity.ActivityUserFavoritesProducts" />
        <activity android:name=".presentation.activity.ActivityAddresses" />
        <activity android:name=".presentation.activity.ActivityOrders" />
    </application>
</manifest>